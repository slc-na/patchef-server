services:
  patchef-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: patchef-server
    image: DAFFODIL.ad.slc.net:5000/slc/patchef-server
    restart: always
    ports:
      - '4000:6970'
    depends_on:
      patchef-database:
        condition: service_healthy
    env_file:
      - .env
    networks:
      - patchef-frontend-network
      - patchef-backend-network

  patchef-database:
    image: postgres:14
    container_name: patchef-database
    restart: always
    ports:
      - '5432:5432'
    env_file:
      - .env
    volumes:
      - patchef-database:/var/lib/postgresql/data
    networks:
      - patchef-backend-network
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  patchef-database:
    name: patchef-database

networks:
  patchef-frontend-network:
    driver: bridge
    external: true
  patchef-backend-network:
    driver: bridge
    external: true
